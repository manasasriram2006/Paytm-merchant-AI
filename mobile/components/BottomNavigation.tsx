import { Pressable, StyleSheet, Text, View } from 'react-native'; import { colors } from '../constants/theme';
export type Tab = 'Home' | 'Inventory' | 'Forecast' | 'Customers' | 'More';
export type Route = Tab | 'AskAI';
const tabs: { key: Tab; icon: string; label: string }[] = [{key:'Home',icon:'⌂',label:'Home'},{key:'Inventory',icon:'▣',label:'Inventory'},{key:'Forecast',icon:'⌁',label:'Forecast'},{key:'Customers',icon:'♙',label:'Customers'},{key:'More',icon:'☰',label:'More'}];
export function BottomNavigation({ activeTab, onChange }: { activeTab: Route; onChange: (tab: Tab) => void }) { return <View style={styles.bar}>{tabs.map(tab => <Pressable key={tab.key} accessibilityRole="tab" accessibilityState={{selected:activeTab===tab.key}} onPress={()=>onChange(tab.key)} style={styles.item}><Text style={[styles.icon,activeTab===tab.key&&styles.active]}>{tab.icon}</Text><Text numberOfLines={1} style={[styles.label,activeTab===tab.key&&styles.active]}>{tab.label}</Text></Pressable>)}</View>; }
const styles=StyleSheet.create({bar:{flexDirection:'row',height:68,backgroundColor:'#FFF',borderTopWidth:1,borderColor:'#E4E9F0',paddingTop:7},item:{flex:1,alignItems:'center',gap:2},icon:{color:'#68768A',fontSize:19,fontWeight:'700'},label:{color:'#68768A',fontSize:10,fontWeight:'600'},active:{color:colors.primary}});
